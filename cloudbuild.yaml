steps:
  - name: gcr.io/cloud-builders/gcloud
    entrypoint: bash
    args:
      - -euo
      - pipefail
      - -c
      - |
          apt-get update -y && apt-get install -y jq
          bash scripts/apply_bq.sh "$PROJECT_ID" 845415315315 sec_v
options:
  logging: CLOUD_LOGGING_ONLY
